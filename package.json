{
  "name": "poetry-blog",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "deploy": "rimraf .next out && next build && gh-pages -d out --dotfiles --nojekyll -b gh-pages"
  },
  "dependencies": {
    "next": "^14.2.26",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "gh-pages": "^6.0.0",
    "postcss": "^8.4.21",
    "rimraf": "^6.0.1",
    "tailwindcss": "^3.3.0"
  }
}
